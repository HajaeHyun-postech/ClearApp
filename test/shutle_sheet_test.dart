import 'package:clearApp/shuttle_menu/data_manage/shuttle_hitsory_handler.dart';
import 'package:clearApp/shuttle_menu/data_manage/shuttle_purchace_history.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:clearApp/util/constants.dart' as Constants;

void main() {
  test('shuttle buy menu data transfer test', () async {
    /*
    List<ShuttlePrchHstr> testList;
    ShuttlePrchHstrHandler instance = ShuttlePrchHstrHandler((list) {
      testList = list;
    });

    await instance.addNewPrchHstr(ShuttlePrchHstr('R1', 1, 1000));
    await instance.addNewPrchHstr(ShuttlePrchHstr('R2', 2, 2000));
    await instance.addNewPrchHstr(ShuttlePrchHstr('R3', 3, 3000));
    await instance.addNewPrchHstr(ShuttlePrchHstr('R4', 4, 4000));
    await instance.addNewPrchHstr(ShuttlePrchHstr('R5', 5, 5000));
    await Future.delayed(Duration(seconds: 5));
    instance.updateRcved('20180673W20200719020220314949');
    instance.updateAppr('20180673W20200719020220321929');
    await Future.delayed(Duration(seconds: 5));
    testList.forEach((element) {
      print(element.toString());
    });

    instance.updateTabChanged(Constants.ShuttleMenuCurrentTab.Admin);

    await Future.delayed(Duration(seconds: 5));
    testList.forEach((element) {
      print(element.key);
    });
    */
  });
}
